language: python
python: 3.6

addons:
  apt:
    packages:
      - make
      - git
    
install:
  - make install

script:
  - make build
  - git add .
  - git commit -m "Build commit $(git rev-parse HEAD)" --author="Travis <grihabor@gmail.com>"
  - git push 
  
  